# Test environment configuration for Playwright tests
# Copy this file to .env.test and adjust URLs as needed

# Service repository locations (optional overrides)
# SSE_GATEWAY_ROOT=/work/ssegateway

# Log streaming for services (optional, useful for debugging)
# PLAYWRIGHT_BACKEND_LOG_STREAM=true
# PLAYWRIGHT_GATEWAY_LOG_STREAM=true
# PLAYWRIGHT_FRONTEND_LOG_STREAM=true