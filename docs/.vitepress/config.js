export default {
  srcExclude: ['features/**', 'epics/**'],
  markdown: {
    // Disables raw HTML in markdown entirely. It works badly with the template
    // syntax used in the command files.
    html: false
  }
}
